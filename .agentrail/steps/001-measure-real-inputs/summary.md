Re-ran Phase 0 against realistic inputs. Original 2026-05-10 measurement profiled only the 15 reg-rs fixtures (peak 406 nodes) and undersized CHUNK_MAX=16, causing dcsno's sno_lex.plsw / sno_exec.plsw to break in production. Used transient peak_nd / peak_chunks instrumentation in src/ast.h and a uart_putstr line in src/main.c after '--- end assembly ---'; temporarily bumped CHUNK_MAX to 64 in src/chunk.h so the bumped binary could observe the real peak without nd_alloc short-circuiting. Drove the instrumented plsw-meas.lgo against all 15 reg-rs fixtures (max 406 nodes / 3 chunks, unchanged), sno_util.plsw (1913 / 15), sno_lex.plsw (5032 / 37), sno_exec.plsw (6008 / 45). sno_engine.plsw was not reachable from this clone; estimated at ~12,953 nodes / ~95 chunks by summing components. Decision recorded in docs/memory-audit-2026-05-12.md: production CHUNK_MAX = 128 (the brief's primary suggestion; 2.85x over sno_exec, 1.54x over the 332 KB pre-Phase-3 floor, 1.35x over the sno_engine estimate). Reverted the instrumentation; src/ files are back to saga-init state; just clean+build-lgo produces a byte-identical 872,174-byte binary; reg-rs 15/15 green.