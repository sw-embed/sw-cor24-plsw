Investigate and fix AND/OR with pointer dereference comparisons causing 'unsupported binary operator' (GitHub issue #5). The expression `(DPTR->DTAG = 0) AND (DPTR->DPAY = 0)` fails at codegen. AND/OR with simple variables presumably works. The issue is likely that the binary operator codegen for AND/OR doesn't handle NODE_DEREF sub-expressions in comparisons. Investigate cg_binop or the expression codegen path for AND/OR to see how operands are evaluated. The fix likely involves ensuring that comparison sub-expressions containing ptr->field derefs produce their boolean result in r0 the same way simple variable comparisons do. Add a test case with AND/OR + ptr->field, and verify the workaround (nested IF) still works.