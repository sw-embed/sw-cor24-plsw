Support %DEFINE NOLISTING to suppress source line comments (GitHub issue #25). In compile_program (main.c), after parse_program returns successfully, check if NOLISTING is defined via def_defined. If so, set cg_listing = 0. This suppresses all source line comments in the assembly output. Add a test that compiles with %DEFINE NOLISTING and verifies no source comment lines ('; N:') appear in the output.