Implemented %INCLUDE directive processing in the lexer. Added include file registry (inc_register/inc_lookup), lexer state stack for nested includes with depth limit 4, lex_try_include() to parse %INCLUDE NAME; syntax with .msw extension fallback. Modified lex_scan() to transparently handle includes (pop on EOF, detect %INCLUDE before operator tokenization). 5 tests: basic include, .msw extension, nested 2-deep, proc defs from includes, % non-include fallback. All pass.