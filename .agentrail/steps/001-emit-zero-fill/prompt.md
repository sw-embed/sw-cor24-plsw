Implement .zero N emission for INIT(0) static arrays per dcpls-emit-zero-fill brief. Modify cg_emit_static_var in src/codegen.h: detect 'all-zero init' (explicit INIT(0), no INIT, or all-zero element list) for BYTE/INT/PTR/WORD/CHAR arrays; emit '.zero <total_bytes>' instead of per-element loop. total_bytes = array_count * element_width (1 for BYTE/CHAR, 3 for INT/PTR/WORD). Mixed init like INIT(0,1,2,0,0) keeps spelled-out form. Add compiler test suite in src/main.c. Verify byte-identical .bin against pre-change output for examples/chain.plsw. Re-bootstrap reg-rs goldens (operator inspection per docs/testing.md). just test 15/15 green. Branch feat/emit-zero-fill already cut; rename to pr/emit-zero-fill via dg-mark-pr.