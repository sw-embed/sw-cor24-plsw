Implemented .zero N emission per dcpls-emit-zero-fill brief. cg_emit_static_var restructured into 3 cases (non-empty string, non-zero numeric, all-zero); the all-zero fast path emits '.zero <total_bytes>' instead of spelled-out '.byte 0,0,...,0'. Byte-identical .bin output verified. Side-effect: fixes a pre-existing bug where INIT(0) on an N-byte array emitted only 3 bytes; new code emits the full width. Added 3 test fixtures in test_static_data (suite 16): 50-byte BYTE array, 50-element INT INIT(0) array, non-empty string init unchanged. just test 15/15 green from clean rebuild; suite 16 reports 0 errors. SNOBOL4 unblocked: sno_main.s drops from 261 KB to ~7 KB once mike reinstalls the new PL/SW compiler.