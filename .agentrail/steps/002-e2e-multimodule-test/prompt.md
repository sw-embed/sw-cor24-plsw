Add an end-to-end PL/SW-driven multi-module test fixture under components/linker/tests/ (or test/modular/). Write 2-3 small .plsw files: one main module and one or two library modules using %DEFINE LIBRARY. Drive the full pipeline from PL/SW source: plsw-compile each module -> meta-gen prep -> cor24-run --assemble (pass 1) -> meta-gen emit -> layout -> cor24-run --assemble (pass 2 with --base-addr) -> link24 -> cor24-run on emulator. Capture expected output and turn this into a regression test (reg-rs entry or shell script). The existing demo-fixup.sh uses hand-written .s; this new test must start from .plsw to validate the compiler half of the pipeline.