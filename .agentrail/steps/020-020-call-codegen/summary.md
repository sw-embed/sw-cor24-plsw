Implemented procedure call code generation. Added cg_count_args() and cg_call() to codegen.h handling full COR24 calling convention: L-to-R eval, pre-allocated stack with sw at sp+i*3, la+jal call, sp cleanup. Updated cg_stmt() and cg_expr() to use cg_call() for NODE_CALL in both statement and expression contexts. Added 10-test suite validating no-arg, single/multi-arg, expression context, sequential, variable args, expression args, nested calls, globals+calls, and call-in-arithmetic. All tests pass.