Built first end-to-end demo: Hello World in PL/SW. Fixed codegen to use label-based static variable references (6 locations) instead of numeric addresses. Fixed calling convention to use push/add sp,N instead of invalid sub sp,rN / sw r0,N(sp). Added runtime emitters (start, UART_PUTCHAR, UART_PUTS), compile_program() pipeline, examples/hello.plsw (9 lines), and test suite 31. Generated hello.s: 106 bytes, 454 instructions, prints 'Hello from PL/SW!' to UART. All 32 test suites pass.