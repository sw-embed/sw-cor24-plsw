Implemented static data emission in codegen.h: string literal table (cg_add_string, cg_emit_string_table, cg_emit_string_bytes), static variable emission (cg_emit_static_var with INIT/zero-fill for word/byte/array), program-level walker (cg_emit_static_data), and string address loading (cg_load_string). 10-test suite validates zero-fill words/bytes, numeric INIT, multiple declarations, zero-fill arrays, string literal registration/emission, multiple string tables, explicit INIT(0), and runtime codegen access. All tests pass on COR24 emulator.