Add double-quoted string literals to the PL/SW lexer (GitHub issue #21). Currently only single-quoted strings ('...') are supported. Add support for double-quoted strings ("...") so that single quotes inside double-quoted strings are ordinary characters and vice versa. Also add delimiter-doubling escape: '' inside '...' produces a literal apostrophe, "" inside "..." produces a literal double-quote. In the lexer (lexer.h), find the string literal scanning code (currently checks for char 39 = single quote). Add a parallel case for char 34 (double quote) with the same logic but using 34 as the delimiter. For delimiter-doubling: when the closing delimiter is seen, peek at the next char -- if it's the same delimiter, consume both and add one to the string. Add tests for: double-quoted strings, single quotes inside double-quoted strings, double quotes inside single-quoted strings, and delimiter doubling.