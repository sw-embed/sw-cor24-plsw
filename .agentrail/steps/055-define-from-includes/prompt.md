Fix %DEFINE value substitution not working from included .msw files (GitHub issue #15). %DEFINE in an include file works for %IF DEFINED() but not for value substitution in the main source. The define table (def_names/def_values) is global, so defines added during include expansion should persist. Investigate whether the issue is actually caused by DEF_MAX overflow (issue #16) rather than an include-specific bug. Test: register an include with %DEFINE FOO 42, then compile source that uses FOO in an expression. If it works after fixing DEF_MAX, close both issues together.