Make scripts/pipeline.sh parallel-safe: (1) replace per-file mktemp /tmp/plsw-XXXXXX.{s,lgo} with a single mktemp -d /tmp/plsw-XXXXXX per invocation, deterministic filenames inside (program.s, program.lgo); (2) drop the auto-rebuild block for build/plsw.lgo and error cleanly when missing or stale -- force callers to build-lgo first. Same mktemp-d treatment for pipeline-dump.sh's scratch (its build/<name>.* outputs stay as-is; they're intentional artifacts). Verify just test passes 5 runs in a row with identical results. Update docs/testing.md with a parallel-safety note. Branch feat/parallel-test-isolation already cut; rename to pr/parallel-test-isolation via dg-mark-pr.