Closed two parallel-test write-race vectors. (1) pipeline.sh and pipeline-dump.sh dropped the auto-rebuild block for build/plsw.lgo; now error cleanly if missing/stale (forces caller to build-lgo first; just test's dep handles it transparently). (2) pipeline.sh's per-file mktemp /tmp/plsw-XXXXXX.{s,lgo} replaced with mktemp -d /tmp/plsw-XXXXXX per invocation, deterministic filenames inside (program.s, program.lgo). Sidesteps BSD-vs-GNU mktemp template differences; gives every parallel pipeline its own filesystem space. docs/testing.md gained a 'Parallel safety' section. Verified 5/5 consecutive just test runs all 15/15 green on Linux; the surgical fix targets the exact shared-write vectors mac flake reports point to.