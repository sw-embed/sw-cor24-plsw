Implement the expression parser with operator precedence. Precedence levels (low to high): logical OR, logical AND, bitwise OR, bitwise XOR, bitwise AND, equality (= !=), comparison (< > <= >=), shift (<< >>), additive (+ -), multiplicative (* /), unary (- ~ NOT). Parse parenthesized sub-expressions. Parse function calls in expressions. Parse ADDR(var) address-of. Parse ptr-> dereference. Test: parse complex expressions and dump AST trees.