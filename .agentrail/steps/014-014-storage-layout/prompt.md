Implement storage layout computation. Walk DCL AST nodes and compute: record sizes and field offsets from level-based declarations (sum of field sizes, aligned). Array sizes (element_size * count). Assign stack offsets for AUTOMATIC variables (negative from fp). Assign static addresses for STATIC variables. Track total frame size per procedure. Test: verify computed sizes and offsets match expected values for sample declarations.