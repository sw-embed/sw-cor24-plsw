Statement parser fully implemented and tested. Parses assignments (simple, expression, field, array subscript), IF-THEN-ELSE, DO-WHILE, DO-counting with TO, CALL with args (including empty args and keyword proc names), RETURN with/without expression, and DO;...END; blocks. Fixed CALL bug where keyword tokens like 'init' were rejected as proc names. All 20 test cases pass on COR24 emulator.