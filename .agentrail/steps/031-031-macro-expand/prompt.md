Implement macro expansion. When lexer encounters ?NAME(KEYWORD(value), ...) invocation, look up NAME in macro table. Match keyword arguments to REQUIRED/OPTIONAL clauses. Substitute argument values into macro body template. For GEN blocks, substitute values into assembly template strings and emit as inline ASM. Expand the result back into the token stream. Test: ?GETMAIN(LENGTH(256), ADDRESS(BUF)) expands to correct code.