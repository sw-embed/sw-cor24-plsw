Replaced 256 KB emit_buf accumulator with 4 KB coalescing flush buffer per dcpls-streaming-emit brief (Option B). emit_char auto-flushes to UART when buffer fills; compile_program calls emit_flush at end; main()'s compile-mode handler drops the trailing uart_putstr(out) since bytes already streamed. emit_output's contract documented as 'unflushed tail only' -- existing test suites that emit small fragments continue to work unchanged. just test 15/15 green; suites 16 (static data) and 29 (macro expansion) report 0 errors. build/plsw.lgo dropped from 2.3 MB to 1.7 MB (~25% smaller, reflecting freed 256 KB of static buffer). Unblocks dcsno's step 003 which sat at 114 bytes free of the old ceiling. CHANGES.md updated with new SHA b6525ebe...8d11 + rebuild command for mike's relay install.