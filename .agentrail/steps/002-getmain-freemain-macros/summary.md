Shipped ?GETMAIN/?FREEMAIN macros wrapping the _PLSW_GETMAIN/_PLSW_FREEMAIN procedures, then iterated on syntax based on user feedback. Final form: PL/I-flavored space-separated parenthetical clauses (?GETMAIN SET(P) LENGTH(N) RC(rc); ?FREEMAIN ADDR(P) LENGTH(N) RC(rc);) -- no outer parens, statement-terminated. Clauses can be reserved-word tokens (e.g. ADDR) since both MACRODEF and invocation parsers now look at cur_text rather than cur_type. RC required in v1 (4=OOM for GETMAIN, passes through 0/1/2 for FREEMAIN). All 5 storage demos rewritten; existing macros (?LED_SET/?GREET/?NOP) and their callers migrated to the same syntax. just test 15/15 green; compiler suite 29 (12 macro tests) 0 errors. docs/storage-allocation.md and the dcsno brief updated. PL/X references scrubbed throughout (project is PL/I-flavored, not PL/X).