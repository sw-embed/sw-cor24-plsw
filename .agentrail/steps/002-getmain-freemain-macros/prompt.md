Step 002 of getmain-freemain-macros (gated on step 001). Define ?GETMAIN(LENGTH(expr), ADDRESS(lvalue), RC(lvalue)) and ?FREEMAIN(LENGTH(expr), ADDRESS(lvalue), RC(lvalue)) in include/_plsw_storage.msw using the new GEN DO source-template form. RC clause is REQUIRED in v1 (avoids needing %IF DEFINED inside templates). IBM-style RC values: GETMAIN 0=success/4=OOM; FREEMAIN passes through _PLSW_FREEMAIN's return (0/1/2). Bodies (with {KEY} substitution): GETMAIN sets ADDRESS, then sets RC based on whether ADDRESS is 0; FREEMAIN sets RC = _PLSW_FREEMAIN(ADDRESS, LENGTH). Rewrite all five storage demos (examples/storage_*.plsw) to use the macro form. Re-bootstrap reg-rs goldens for the five plsw_storage_* cases. Update docs/storage-allocation.md (Macro layer flipped to 'shipped'); remove deferral note from _plsw_storage.msw header. Update tools/briefs/dcsno-storage-allocation-runtime.md to mention the macro spelling is available. just test must be 15/15 green. dg-mark-pr at end.