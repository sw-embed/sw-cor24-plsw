Implement expression code generation. Literal loading via mvi. Variable load/store via lw/sw with stack offsets. Binary arithmetic: add, sub, mul instructions. Software divide routine (no hardware div on COR24). Comparison operators generating conditional branch sequences. Register allocation for expression temporaries using r0, r1, r2 with spill to stack when pressure exceeds 3. Test: compile expressions, run on emulator, verify results via UART output.