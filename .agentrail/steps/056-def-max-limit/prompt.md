Increase DEF_MAX from 32 to 128 and add a warning when the limit is reached (GitHub issue #16). DEF_MAX in lexer.h line 89 limits %DEFINE entries to 32. The SNOBOL4 project has 37+ defines across 3 includes. Find DEF_MAX in lexer.h, increase to 128. Also add a warning in def_set when def_count >= DEF_MAX. Verify the compiler builds and test %DEFINE from includes works after the increase.