Fix DCL inside ELSE IF THEN DO blocks causing 'undefined variable' (GitHub issue #23). Issue #11 added layout_locals recursion into NODE_IF (then=nd_right, else=nd_ival). The ELSE IF chain creates nested IF nodes: the outer IF's nd_ival points to an inner IF node (not a BLOCK). When layout_locals recurses into the else body, it expects a BLOCK node with nd_left children, but the inner IF's then-body is at nd_right and its else at nd_ival. The fix: layout_locals already recurses into NODE_IF via nd_right and nd_ival. Check if the ELSE IF creates the inner IF as a direct child or wrapped in a BLOCK. Reproduce with the example from the issue, add debug output if needed, then fix the recursion to handle the ELSE IF nesting pattern.