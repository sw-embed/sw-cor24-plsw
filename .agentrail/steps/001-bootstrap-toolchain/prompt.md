Bootstrap PL/SW into the shared toolchain per /disk1/github/softwarewrighter/devgroup/tools/briefs/dcpls-bootstrap-plsw-toolchain.md. Three deliverables: (1) produce build/plsw.lgo as the canonical shippable via 'just build-lgo'; (2) migrate every cor24-run --run / cor24-run --assemble callsite in justfile, scripts/pipeline.sh, scripts/pipeline-dump.sh, components/linker/tests/demo-fixup.sh, and components/linker/tests/demo-plsw-modular.sh to the new cor24-asm + cor24-emu --lgo pipeline (mapping table in the brief); (3) add 'just install-layer1' that stages link24 and meta-gen to dist/bin/ for mike's orchestrator. Also fix the hardcoded sw-vibe-coding/tc24r path in justfile:4 (tc24r is now under sw-embed/sw-cor24-x-tinyc). No PL/SW compiler logic changes; no cross-repo updates; no installing into work/bin/. Update README to document the new artifact paths. Verification gauntlet from the brief: just clean; just build; just build-lgo; just run-input '...'; just test; just hello-macro; just chain; just install-layer1; plus components/linker/tests/demo-fixup.sh and demo-plsw-modular.sh. Branch: cut feat/bootstrap-toolchain off dev (already done by dg-new-feature), rename to pr/bootstrap-toolchain at end via dg-mark-pr.