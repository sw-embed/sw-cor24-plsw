Fix AST pool exhaustion causing misleading parse errors (GitHub issue #6). The 256-node AST pool is too small for real programs. When nd_alloc returns NODE_NULL, the parser continues with null nodes producing confusing errors like 'expected ), got ->'. Two fixes needed: (1) Increase AST_MAX from 256 to 512 or 1024 in ast.h -- each node is small so this is cheap. (2) Ensure nd_alloc failure produces a clear 'AST pool exhausted' error that halts parsing immediately instead of cascading into misleading errors. Check ast.h for AST_MAX and nd_alloc, then check all callers to ensure they handle NODE_NULL properly. Verify the compiler still builds and existing examples work after the increase.