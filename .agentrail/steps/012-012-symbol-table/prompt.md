Implement the scoped symbol table. Support global scope and per-procedure scopes. Operations: sym_enter_scope(), sym_exit_scope(), sym_insert(name, type, storage, ...), sym_lookup(name). Track: name, type tag, width, storage class, stack offset or static address, declaration level, scope depth. Max 64 symbols per scope (linear scan). Test: nested scopes resolve correctly, shadowing works, undefined variable detected.