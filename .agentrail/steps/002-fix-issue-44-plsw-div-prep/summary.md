Fixed issue #44: parse_la_external in meta-gen prep rejected '__plsw_*' symbols because it required the second char to be alphabetic. Relaxed the check to accept identifier-continue chars (letter/digit/underscore), so PL/SW runtime helpers like __plsw_div now rewrite to 'la rN,0' and emit FIXUP entries. Added 5 unit tests (user external, runtime helper, local symbol, numeric immediate, end-to-end prep). Verified tests/demo-fixup.sh still passes. Committed 19346b4 and pushed.