Phase 0 measurement: instrumented nd_alloc with peak_nd tracking + main.c with 'AST peak: N' line; ran instrumented compiler against all 15 reg-rs fixtures via /tmp/measure-peak.sh (compile-only, no assemble/run). Max peak: 406 nodes (storage_coalesce). Min: 8 (hello). All 15 fits in 16-chunk × 136-node = 2,176 cap with 5.4× headroom. Decision recorded in docs/memory-audit-2026-05-10.md: keep CHUNK_MAX=16; bump only if dcsno/dcftn observed-overflow (out-of-repo). Reverted instrumentation (production source unchanged). Verified: just clean+build-lgo OK; reg-rs 15/15 green.