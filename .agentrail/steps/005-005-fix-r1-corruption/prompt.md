Investigate and fix GitHub issue #31: function epilogue pops r1=0 causing jmp to PC=0, re-entering _start. Triggered by SNOBOL4 programs that call PUT_DEC (factorial, fizzbuzz, remdr) but not by hello.sno. Trace shows pop r1 yielding 0 inside _READ_SRC during MAIN unwind. Likely a stack-imbalance in some prologue/epilogue codegen path or in UART_PUTCHAR's wrapper that drifts sp by a small amount per call. Repro is in sw-embed/sw-cor24-snobol4 at e7257c3 via scripts/build-modular.sh + cor24-run on fizzbuzz.sno. Find the codegen drift, fix it, and verify factorial/fizzbuzz/remdr all halt cleanly without the inline ASM workaround. Close issue #31.