Added IF/THEN/ELSE code generation (cg_if) and recursion tests. The existing frame management already supported recursion correctly -- each call gets its own frame via prologue/epilogue. Added 3 tests: recursive factorial (FACT with self-call), nested calls (ADD(MUL,MUL) with call results as args), and doubly-recursive fibonacci (FIB(N-1)+FIB(N-2) with push/pop for intermediate results). All tests pass.