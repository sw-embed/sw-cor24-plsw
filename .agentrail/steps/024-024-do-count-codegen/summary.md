Implemented DO I = start TO end code generation. Added cg_do_count() in codegen.h that lowers counted loops to while-equivalent: assign start to loop var, emit header label, compare I vs end via cls r1,r0 with push/pop to preserve I across end expr eval, branch to end if I > end, emit body, increment I, jump back. Added NODE_DO_COUNT dispatch in cg_stmt(). Three tests: simple 1-TO-10 with CALL, expression bounds (parameter end), nested DO count + DO WHILE. All tests pass.