Implement the PL/SW automated test harness. Deliverables: (1) Fix stat -f%z portability bug to stat -c%s (or 'wc -c <' equivalent) in components/linker/tests/demo-fixup.sh and demo-plsw-modular.sh. (2) Adopt the reg-rs convention used across sw-cor24-* siblings: tests/expected/<name>.{out,rgt} files for each .plsw example, scripts/test.sh wrapping 'reg-rs run -p plsw_ --parallel'. (3) just test recipe runs both the linker demos and the reg-rs example suite. (4) just test-bootstrap-goldens recipe regenerates goldens from the current toolchain (used when first creating the baseline or when an intentional behavior change lands). (5) docs/testing.md documents the harness, bootstrap workflow, and the known tc24r blocker. (6) README updated to reference the harness. Verification: linker demos pass after stat fix; harness skeleton runs cleanly (skips/passes appropriately while tc24r blocker is open); no other regressions. Out of scope: fixing the tc24r parse error on src/main.c (separate dcxtc brief), changes to PL/SW compiler logic, changes to cor24-* tooling. Branch: feat/plsw-test-harness already cut; rename to pr/plsw-test-harness via dg-mark-pr at end.