# Continuation note — 2026-05-23

Mac session was paused mid-verification because the locally-installed
`tc24r` is stale. Pick this up on a system with a newer toolchain.

## Repo state at handoff

- Branch: `dev`, working tree clean.
- HEAD: `33b21b1 Merge dcpls/pr/baseline` (matches `origin/dev`).
- Active saga: `capacity-and-test`, **step 003 `[>] baseline`** in progress.
  Not the work of this session — do NOT pick it up here unless directed.

## What this session set out to do

Verify that the **reg-rs `--parallel` mode flake** I diagnosed earlier
(BSD-mktemp suffix race in `scripts/pipeline.sh`) is actually fixed
on `origin/dev`.

Before the pull, local `dev` was 39 commits behind `origin/dev`. On
that stale snapshot the parallel race was real — `just test` would
fail a different random 5–6 of 15 tests on each run with
`mktemp: mkstemp failed on /tmp/plsw-XXXXXX.s: File exists`. The
`record-fix` PRINT_STR change committed at `0232957` was confirmed
visible in `reg-rs/plsw_record.out` (labels and values on the same
line — that fix is good).

## Fix is already on origin/dev — verification pending

`scripts/pipeline.sh:114-117` (current `origin/dev`):

```bash
SCRATCH=$(mktemp -d /tmp/plsw-XXXXXX)
trap "rm -rf $SCRATCH" EXIT
TMPASM="$SCRATCH/program.s"
TMPLGO="$SCRATCH/program.lgo"
```

The header comment on lines 106–113 explicitly cites
"BSD-vs-GNU mktemp differences with trailing extensions" — same
root cause I'd identified independently. So no code change is
needed; just verify on a working build:

```sh
just build-lgo                    # must succeed first
just test                         # expect 15/15 in --parallel
just test                         # run 2–3 more times to rule out residual races
REG_RS_DATA_DIR="$PWD/reg-rs" \
    reg-rs run -p plsw_           # serial sanity check, expect 15/15
```

## The blocker on this Mac

`just build-lgo` fails:

```
tc24r src/main.c -o build/plsw.s -I .../sw-cor24-x-tinyc/include -I src
tc24r: error at offset 35864: expected type, got Star
```

- Installed binary: `/Users/mike/.local/bin/tc24r`,
  mtime `Apr  4 16:59`, size `1188048`.
- Project sources (`src/main.c`, `src/{ast,chunk,codegen,compile,
  emit,layout,parser,types}.h`, plus the new `src/test_main.c`)
  were last touched `May 18`. The recent `ast-to-chunks` and
  `capacity-and-test` saga work added `chunk.h`, `compile.h`,
  `test_main.c`, and a `build-test` / `build-test-lgo` lane in
  the justfile. Something in that newer C apparently uses a
  construct the April-vintage tc24r doesn't yet parse.
- Offset 35864 is past EOF for `main.c` alone (7617 bytes), so the
  failure is somewhere in the included headers. By rough cumulative
  arithmetic it lands inside `src/ast.h`. Not pinned down precisely
  in this session — and shouldn't be, since the answer is
  "update tc24r," not "patch the source."
- Upstream tc24r lives at `~/github/sw-embed/sw-cor24-tinyc`
  (per CLAUDE.md). Rebuild + install from there to clear the
  blocker.

## Unwind that happened during this session (no commits)

1. Ran `agentrail snapshot` against the stale local state:
   `refs/agentrail/snapshots/20260518T035033 = 026fdc92839d`.
   Kept as a safety net; nothing depends on it now.
2. Archived the completed `record-fix-and-test-rigor` saga and
   `agentrail init`'d a new `fix-parallel-mktemp-race` saga with
   step `001-fix-mktemp-race`. Created a `fix/parallel-mktemp-race`
   branch off stale `dev`.
3. Immediately after `git fetch` showed `origin/dev` was 39 commits
   ahead and already contained the mktemp fix in `pipeline.sh`,
   the entire new saga was redundant. It was unwound by:
   - `git stash push --include-untracked -m redundant-saga-...`
   - `git switch dev && git merge --ff-only origin/dev`
   - `git stash drop`
   - `git branch -D fix/parallel-mktemp-race`
4. Net effect: zero commits made, zero files modified in tracked
   state. `.agentrail/` matches `origin/dev` exactly (the
   capacity-and-test saga, step 003 in progress).

If `agentrail audit` flags anything from this, the snapshot ref
above captures the intermediate state. Nothing was lost.

## Things to do on the updated system

In priority order:

1. **Update tc24r** (rebuild/install from `~/github/sw-embed/sw-cor24-tinyc`)
   until `just build-lgo` succeeds on the current `src/main.c`.
2. **Run `just test` 3–5 times** to confirm parallel mode is
   green. If it ever fails, capture the actual stderr with
   `REG_RS_DATA_DIR="$PWD/reg-rs" reg-rs run -p plsw_ --parallel -vv`
   to see the per-test failure reason (the `.out`/`.err` files
   you'd see in `reg-rs/` get overwritten by the latest run, so
   inline `-vv` is the reliable way to inspect a parallel failure).
3. **Resume the active saga** if that's what you intended to do:
   `agentrail next` to see step 003-baseline's prompt, then work
   it on a new `feat/baseline` (or whatever slug) branch per
   CLAUDE.md.

## Things explicitly NOT done this session

- No commits, no push, no PR, no merge.
- No changes to `scripts/pipeline.sh` (fix already exists upstream).
- No changes to `src/`, `docs/`, `justfile` (justfile only changed
  via the `ff` from `origin/dev`; that change is upstream's, not mine).
- No work on saga step 003-baseline.
- No tc24r rebuild attempted from this Mac.

## Memory notes saved this session

- `project_devgroup_clone_privileges.md` — Mac clone has full gh
  privileges; the `dg-*` helper / coordinator policy in CLAUDE.md
  only applies to the unprivileged Arch dcpls clone. Indexed in
  `MEMORY.md`. Relevant if a future session wonders why the
  `dg-*` helpers aren't on PATH here.
