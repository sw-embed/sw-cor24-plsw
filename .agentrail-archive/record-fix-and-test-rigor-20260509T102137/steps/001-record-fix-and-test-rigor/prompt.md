Fix examples/record.plsw to compile (it uses ADDR('literal') which the current codegen rejects). Use the web-ui shape (pre-declared CHAR labels + PRINT_STR helper that walks NUL-terminated buffer via UART_PUTCHAR so labels don't auto-newline) but with the original 100/200 values. Harden scripts/bootstrap-goldens.sh: after each reg-rs create, print exit_code and stdout line count; abort if any case has non-zero exit code unless --allow-failures passed; print 'REVIEW BEFORE COMMITTING' banner. Update docs/testing.md to add an 'Operator review' section making the inspection requirement explicit -- goldens are not trustworthy until a human reviewed each .out/.err/.rgt triple for sanity. Document the plsw_record incident as the worked example. Re-bootstrap goldens for all 15 cases, INSPECT each one before committing. Verify .out non-empty, .rgt exit_code=0, content matches expectation. just test must be 15/15 green from clean rebuild. Branch feat/record-fix-and-test-rigor already cut; rename to pr/record-fix-and-test-rigor via dg-mark-pr at end.