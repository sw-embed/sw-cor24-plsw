Fixed examples/record.plsw (was failing to compile due to ADDR('literal'); retrofitted from web-sw-cor24-plsw with PRINT_STR helper using BASED single-byte traversal so labels and values share a line). Hardened scripts/bootstrap-goldens.sh to print per-case exit_code + stdout line count and refuse success on non-zero exit codes (--allow-failures override). Updated docs/testing.md with explicit 'Operator review (REQUIRED)' section documenting the plsw_record incident. Re-bootstrapped all 15 reg-rs goldens with operator inspection: plsw_record now produces correct output; plsw_chain and plsw_led are intentionally empty; all others non-empty matching demo intent. plsw_select_nested has similar label-split bug as old record but is out-of-scope per user direction. just test 15/15 green from clean rebuild.