Replace 256 KB emit_buf with 4 KB coalescer per dcpls-streaming-emit brief (Option B). Modify src/emit.h: EMIT_BUF_SIZE=4096; add emit_flush (drain to UART via uart_putchar loop, reset emit_pos); emit_ch flushes when buffer full; document emit_output's new 'unflushed tail only' contract. Modify src/main.c: add emit_flush() at end of compile_program (still returns emit_output for NULL-on-failure contract); remove uart_putstr(out) in main()'s compile-mode handler (bytes already streamed). Verify just test 15/15 green; compile suites (16 static data, 29 macro expansion) still pass. Add CHANGES.md entry with new SHA so mike knows to rebuild+reinstall plsw.lgo. Branch feat/streaming-emit cut; mark-pr at end.