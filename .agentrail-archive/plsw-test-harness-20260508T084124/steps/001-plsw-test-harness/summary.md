Built the reg-rs regression harness scaffolding. Added tests/driver.sh (per-case dispatcher into pipeline.sh with the right .msw includes), scripts/test.sh (wraps reg-rs run with REG_RS_DATA_DIR set to <repo>/reg-rs), scripts/bootstrap-goldens.sh (wraps reg-rs create for each case). Fixed the stat -f%z portability bug in components/linker/tests/demo-{fixup,plsw-modular}.sh to wc -c < (BSD->Linux). Added justfile recipes: test, test-bootstrap-goldens, test-linker, smoke (renaming the old test). Wrote docs/testing.md covering layout, run/bootstrap workflows, linker test caveats, and the tc24r blocker status. Updated README's Build System table and Documentation list. Surfaced two tc24r limitations as dcxtc briefs (the existing array-size-expressions one stays; new string-literal-concatenation brief written) -- per dogfooding policy, no PL/SW source workarounds. Bootstrap of golden files is gated on those tc24r fixes landing.