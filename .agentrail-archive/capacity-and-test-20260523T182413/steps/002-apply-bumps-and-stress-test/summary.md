Applied both capacity bumps and shipped the regression test that the prior saga's step 1 should have generated. src/chunk.h: CHUNK_MAX 16 -> 128 (512 KB pool, 2.85x over sno_exec's 45-chunk peak, 1.54x over the 332 KB pre-Phase-3 static AST floor). src/main.c: SRC_BUF_SIZE 65536 -> 196608 (192 KiB, dcsno's brief interim value, unblocks the 137 KB sno_engine.plsw consolidation). Created examples/chunk_stress.plsw -- a 2,213-line synthetic fixture (80 DCLs + 60 PROCs * 30 stmts + MAIN) measured at 12,028 peak nodes / 89 peak chunks during compile. Confirmed the fixture FAILS with the broken CHUNK_MAX=16 ('AST pool exhausted (chunk table full)' at line 478) and PASSES with 128 -- it would have caught the original regression. Compile-only runner scripts/run-chunk-stress.sh bypasses pipeline.sh because the fixture takes ~620M instructions (over the 200M default). Wired into tests/driver.sh + scripts/bootstrap-goldens.sh + reg-rs/plsw_chunk_stress.{rgt,out}. Spot-checked sno_lex.plsw (10,569 lines of .s) and sno_exec.plsw (12,043 lines of .s) -- both compile clean against the bumped binary; both failed before. plsw.lgo size: 872,174 -> 2,185,150 bytes (the .lgo encoding adds ~2x overhead for the .zero chunk_storage region; runtime SRAM footprint is fine). reg-rs 15/15 -> 16/16 green.