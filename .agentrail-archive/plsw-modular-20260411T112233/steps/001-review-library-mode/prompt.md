Review the in-progress LIBRARY mode suppression already present as uncommitted changes in src/main.c and src/codegen.h. The changes implement %DEFINE LIBRARY which suppresses (a) runtime preamble emission in compile_program, (b) MAIN procedure emission in cg_program_procs, and (c) all top-level DCL data emission in cg_emit_static_data. Verify against docs/linker-design.md. Build the compiler with 'just build' and confirm both normal and LIBRARY-mode programs compile cleanly. Then commit the changes with a clear message referencing GitHub #28.