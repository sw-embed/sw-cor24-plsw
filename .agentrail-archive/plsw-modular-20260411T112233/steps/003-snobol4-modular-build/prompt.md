Switch the SNOBOL4 interpreter (~/github/sw-embed/sw-cor24-snobol4) to a modular build using LIBRARY mode. Per docs/linker-design.md, only the main module emits the runtime, MAIN wrapper, and shared globals from snoglob.msw / descr.msw / heap.msw etc.; library modules begin with %DEFINE LIBRARY; and reference shared globals as externals (resolved by FIXUP). Convert any module-private top-level DCLs in library modules into PROC-local STATIC variables. Add a build script and verify the dating-app demo runs end-to-end on the emulator. Document any compiler limitations encountered and file follow-up issues if needed.