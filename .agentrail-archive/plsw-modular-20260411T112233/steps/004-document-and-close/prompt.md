Add a 'Modular compilation' section to docs/usage.md that introduces %DEFINE LIBRARY, summarizes the suppression rules (runtime, MAIN, top-level DCLs), explains the shared-globals-owned-by-main rule, and links to docs/linker-design.md for the full pipeline. Cross-reference the new e2e test from step 002 as a worked example. Then close GitHub issue #28 with a comment summarizing the implemented design (note that it diverges from the literal issue text: FIXUP-based, no jump tables, no -m flag, no EXTERNAL keyword) and link to the relevant commits and the SNOBOL4 modular build from step 003.