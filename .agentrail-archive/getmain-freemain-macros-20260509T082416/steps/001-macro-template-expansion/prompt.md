Step 001 of getmain-freemain-macros. Extend MACRODEF to support source-text template expansion (the new GEN DO form) alongside assembly-template expansion (renamed to GEN ASM DO). NO NEW KEYWORDS -- GEN, ASM, DO, END already exist. Backward compat with existing GEN DO is broken (educational project; user authorized): migrate ?LED_SET, ?GREET, ?NOP to GEN ASM DO. Lexer captures source-text spans for macro args (offset+length into input buffer). MACRODEF body parser distinguishes GEN DO (source template, captured as a span) from GEN ASM DO (assembly template, existing per-line behavior). Macro expander substitutes {KEY} placeholders with captured arg spans, re-tokenizes the result, splices into the input stream. Plumb a depth parameter through the expand function (always called with depth=1 in v1; sets the foundation for future nested ?MACRO calls with a 32-level recursion limit). Implementation must NOT preclude future %IF/%ELSE/%DO inside templates -- treat the template body as inert text in v1 with substitution only. Add compiler-internal test suites in src/main.c covering literal LENGTH, runtime-variable LENGTH, simple-identifier ADDRESS, and span re-use (one arg referenced twice in body). Existing macros after migration: just test stays 15/15 green; hello-macro and chain demos keep working end-to-end. See docs/storage-allocation.md and src/macro.h for context.