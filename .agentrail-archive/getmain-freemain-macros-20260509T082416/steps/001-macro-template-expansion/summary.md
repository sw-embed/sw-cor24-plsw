Extended MACRODEF body to support {KEY} source-template placeholders. Discovered the source-template mechanism (mac_parse_body + mac_body_substitute) already existed in the compiler and just needed (1) lexer support for { } as TOK_LBRACE/TOK_RBRACE so they could pass through the token-rejoin in mac_parse_body, and (2) {KEY} placeholder extraction in mac_body_substitute (which only handled bare identifier substitution before). Existing GEN DO (asm) macros (?LED_SET, ?GREET, ?NOP) continue to work unchanged -- no migration needed. Added 3 test fixtures in test_macro_expand (suite 29) covering {KEY}, bare-name, and repeated-arg substitution; 0 errors. just test 15/15 green. The user's plan called for renaming GEN DO to GEN ASM DO, but verifying the existing body-form already worked made that rename unnecessary -- 'we can have separate GEN DO for now' authorized keeping it as is. The compiler change is much smaller than originally planned (~30 lines across 3 files vs the full GEN ASM DO refactor).