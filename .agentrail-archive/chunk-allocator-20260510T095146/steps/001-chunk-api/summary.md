Scaffold src/chunk.h: 4 KB chunk allocator (CHUNK_SIZE=4096, CHUNK_MAX=16, 64 KB pre-reserved pool) with chunk_init/alloc/free/used. Header included from main.c + test_main.c; both build-lgo and build-test-lgo succeed. chunk_storage lands at line 35616 of build/plsw.s, 21,846 .word lines = 65,538 bytes physical (the only static block exceeding the 4 KB ceiling, marked /* lint-exempt: chunk-pool */). No callers yet - migration is the next saga (ast-to-chunks). Production plsw.lgo grew from 1,511,582 to 1,657,430 bytes (+~145 KB - pool reservation plus chunk_table + function code; expected, recorded in step 003 baseline).