Capture post-chunk-allocator baseline and close the chunk-allocator saga. plsw.lgo: 1,511,582 -> 1,657,430 bytes (+145,848 / +14%, SHA 712fb0be...). plsw_test.lgo: 1,663,430 bytes (SHA b829f286...). chunk_storage emits as .zero 65536 (single directive); chunk_table as .zero 96; chunk_init/alloc/free/used DCE'd from production (no callers). Updated docs/shrink-lgo-size.md Phase 2 -> DONE with measurements and a Phase 2b section pointing at the next saga (ast-to-chunks) for the ~296 KB net reclaim. Added CHANGES.md entry flagging this as a parity-only rebuild (no semantic change; downstream consumers don't need to rebuild).