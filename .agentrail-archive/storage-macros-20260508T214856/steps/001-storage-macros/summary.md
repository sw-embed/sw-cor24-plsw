Shipped include/_plsw_storage.msw with _PLSW_GETMAIN(SIZE) and _PLSW_FREEMAIN(USERADDR, LEN) free-list-backed procedures over a configurable heap (default 64KB). 6-byte block header (size + next-or-magic); first-fit allocation with split when remainder is useful; forward coalesce on free; alloc-magic 0xFFFFFF in BLOCK_NEXT detects double-free; LEN cross-check against in-band size catches caller bookkeeping bugs (returns RC=2). Lazy init on first call. PLSW_STORAGE_HEADERS_ONLY guard for multi-module use. Five reg-rs demos + goldens (basic/coalesce/oom/double_free/size_mismatch); just test 15/15 green. ?GETMAIN/?FREEMAIN macro spelling deferred (PL/SW MACRODEF emits assembly only; can't synthesize correct code for runtime-variable LENGTH or stack-frame-local ADDRESS); documented as a future saga gated on AST-level macro expansion. Updated docs/storage-allocation.md to v1-shipped state and updated the dcsno brief to reflect that the substrate is now usable.