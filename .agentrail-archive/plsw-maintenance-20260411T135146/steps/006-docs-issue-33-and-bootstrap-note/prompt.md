Bundle two cleanups: (1) commit the 'Known Limitations' section in docs/usage.md that documents GitHub issue #33 (long IF/ELSE-IF chain stack-exhaustion limit, with the SELECT/WHEN workaround); (2) add a paragraph to CLAUDE.md noting the saga-init self-orphan pattern is an expected artifact, not a bug to chase. Also retroactively binds the 3160803 saga-init chore commit to this step.