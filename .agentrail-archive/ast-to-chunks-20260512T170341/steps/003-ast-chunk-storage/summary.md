Moved the AST node pool from 10 static parallel arrays (nd_*_arr[NODE_POOL_MAX]) to a struct-of-arrays ast_block backed by chunk_alloc(). New layout: AST_NODES_PER_CHUNK=136 slots per 4 KB chunk (10 fields x 136 x 3 bytes = 4080, with 16 bytes slack), AST_CHUNKS_MAX=CHUNK_MAX=16, effective cap 2176 nodes (5.4x headroom over the 406-node peak recorded in step 1). The accessor macros from step 2 absorbed the change in one place each so no callsite edits were needed. ast_init() now frees owned chunks back to the pool; nd_alloc() grows the chunk table lazily. Wired chunk_init() into compile_program() (compile.h) and the test main() (test_main.c) so chunk_table[].base is set up before first chunk_alloc. plsw.lgo dropped from 1,657,430 to 872,174 bytes (785 KB, ~47%) -- well past the saga's ~296 KB projection. Reg-rs 15/15 green; emulator-run suites 4/5/14/17/36/37 all 0 errors; all-suite run confirms chunk reset hygiene across 30+ ast_init cycles. Pre-existing streaming-emit failure on compile-roundtrip suites 31-35 is unchanged (verified against baseline). Static nd_*_arr arrays confirmed absent from build/plsw.s.