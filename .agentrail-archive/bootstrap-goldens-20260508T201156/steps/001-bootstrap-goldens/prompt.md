Bootstrap reg-rs goldens for the PL/SW regression suite per tools/briefs/dcpls-bootstrap-goldens.md. Steps: (1) add 'mkdir -p build' to the justfile build recipe so fresh clones don't fail. (2) just clean && just build && just build-lgo, confirm build/plsw.lgo exists and is >1MB. (3) just test-bootstrap-goldens, capture all 10 goldens (hello, led, loop, record, define, select_demo, select_nested, macro, hello_macro, chain). (4) just test, verify all 10 pass. (5) optional: just test-linker, document any pre-existing failures in docs/testing.md. (6) commit reg-rs/plsw_*.{rgt,out} + justfile + optional docs/testing.md update. Out of scope: compiler logic changes, fixes to demo-fixup.sh, new test cases. Branch: feat/bootstrap-goldens already cut; rename to pr/bootstrap-goldens via dg-mark-pr at end.