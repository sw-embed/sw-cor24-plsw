Step 002 of getmain-freemain-macros saga (gated on step 001 shipping). Define ?GETMAIN(LENGTH(expr), ADDRESS(lvalue)) and ?FREEMAIN(LENGTH(expr), ADDRESS(lvalue)) macros in include/_plsw_storage.msw using the EXPAND DO mechanism from step 001. Bodies: '{ADDRESS} = _PLSW_GETMAIN({LENGTH});' and 'CALL _PLSW_FREEMAIN({ADDRESS}, {LENGTH});'. Rewrite examples/storage_basic.plsw, storage_coalesce.plsw, storage_oom.plsw to use ?GETMAIN/?FREEMAIN. storage_double_free.plsw and storage_size_mismatch.plsw keep the procedure form for the calls that need RC inspection (no RC clause in the macro v1). Re-bootstrap reg-rs goldens for all 5 storage_* cases. Update docs/storage-allocation.md ('Macro layer (deferred)' -> 'Macro layer (shipped)'; update contract examples). Remove the deferral note from include/_plsw_storage.msw's header comment. just test must be 15/15 green. dg-mark-pr at the end (after this step is complete).