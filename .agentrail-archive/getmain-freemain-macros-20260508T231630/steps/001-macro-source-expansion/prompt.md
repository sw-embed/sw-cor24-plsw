Step 001 of getmain-freemain-macros saga. Extend PL/SW's macro system to support source-level (token-level) expansion in addition to the existing assembly-level GEN DO. Add EXPAND DO construct: lexer recognizes TOK_EXPAND; macro arg parsing captures source-text spans; macro expander substitutes {KEY} placeholders with captured spans and re-feeds the result through the lexer/parser for the surrounding parse. Add compiler-internal test suites in src/main.c covering literal LENGTH, expression LENGTH, simple-identifier ADDRESS, and (best-effort) complex lvalues. GEN DO macros (?LED_SET, ?GREET, ?NOP) must continue to work unchanged -- backward compat is non-negotiable. just test stays 15/15 green. No consumer changes yet; that's step 002. Read docs/storage-allocation.md for the design context and src/macro.h for the existing MACRODEF implementation. Branch feat/getmain-freemain-macros already cut; do not mark-pr until step 002 also lands.