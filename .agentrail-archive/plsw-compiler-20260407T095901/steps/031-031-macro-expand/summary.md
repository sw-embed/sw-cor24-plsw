Implemented macro expansion engine in macro.h. Added mac_parse_invoke() to parse ?NAME(KEYWORD(value),...) invocations, mac_gen_substitute() to replace {CLAUSE_NAME} placeholders in GEN template lines and wrap as ASM DO blocks, mac_body_substitute() for body text clause substitution, and mac_invoke() as the top-level API. 7 tests in suite #29 cover basic expansion, missing required rejection, unknown macro rejection, body substitution, multiple GEN blocks, optional clauses, and re-lexing verification. All pass.