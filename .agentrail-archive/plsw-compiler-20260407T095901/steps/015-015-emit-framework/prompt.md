Implement the assembly emitter framework. Output buffer with emit_str(), emit_line(), emit_label(), emit_instr(), emit_comment() functions. Section management: emit_text_section(), emit_data_section(). Unique label generation for branches (L0, L1, ...). Source-line comment tracking (emit original PL/SW line as comment before generated code). Test: emit a skeleton .s file with sections, labels, and comments. Verify cor24-run can assemble it.