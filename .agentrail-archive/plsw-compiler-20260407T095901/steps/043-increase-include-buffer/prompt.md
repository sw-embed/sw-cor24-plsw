Increase the include buffer size from 12KB to a larger value (GitHub issue #3). The current INC_BUF_SIZE of 12288 bytes is too small for multi-module projects. The SNOBOL4 project already has 3 include files totaling ~5KB in compact form, and before trimming comments they were ~15KB which triggered 'ERROR: include buffer overflow'. The COR24 has 1MB SRAM so 12KB is overly conservative. Find INC_BUF_SIZE in src/main.c and increase it to 32768 (32KB). This is a simple constant change. Verify the compiler still builds and existing examples work after the change.