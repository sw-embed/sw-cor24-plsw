Implemented record field access code generation. Added sym_tdesc[] to symtab for tracking type descriptors per symbol, layout_last_tdesc bridge in layout.h, and cg_field_addr/cg_field_load/cg_field_store in codegen.h. Handles NODE_FIELD_ACCESS in both cg_expr() (field reads) and cg_assign() (field stores) with correct width-based load/store (lb/sb for BYTE, lw/sw for WORD/INT). Added 3 test cases covering field assign/read, mixed-type offset correctness, and field values in call arguments. All tests pass.