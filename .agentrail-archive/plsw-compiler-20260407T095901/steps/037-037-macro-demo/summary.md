Integrated macros into the compilation pipeline. Parser now handles MACRODEF at top level (calls mac_parse_def) and ?MACRO() in statements (calls mac_invoke, pushes expansion onto lexer include stack). Added mac_init to compile_program. Created examples/system.msw with LED_SET and NOP macros, examples/macro.plsw demo that %INCLUDEs the .msw and invokes macros. End-to-end verified: 'Macro demo OK' output, LED toggle, inline ASM from GEN blocks. Also fixed bc->brt and jmp->bra branch mnemonics for COR24 assembler compatibility.