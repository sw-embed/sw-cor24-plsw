Implemented MACRODEF parsing in src/macro.h: parses MACRODEF name; REQUIRED/OPTIONAL clauses with typed params (expr/lvalue/label); GEN DO template strings; body text (IF/THEN/ELSE); END. Flat parallel-array storage for tc24r compat. Macro table with lookup, parse_def, parse_file, dump. 5 tests all pass (GETMAIN with 4 clauses + GEN, lookup, multi-macro file, body capture, label clause type). Also refactored main() into suite-selectable test runner (0-28) to work within emulator instruction limits.