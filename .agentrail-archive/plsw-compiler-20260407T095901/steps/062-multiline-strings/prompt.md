Support multi-line string literals in CHAR INIT (GitHub issue #22). Currently strings spanning 3+ lines cause parse errors. The lexer already handles newlines within strings (it doesn't stop at newline), but the source line tracking (lex_line increment on newline char 10) may be interfering, or the INIT parser may have a line-based limit. Investigate: (1) does the lexer correctly scan a 3-line string? Test with lex_init + lex_scan directly. (2) if the lexer works, check parse_init_attr for any limits. The fix may be as simple as ensuring the string scanner doesn't break on newlines. Add a test with a 3+ line INIT string.