Implement inline assembly support. Parse ASM DO; ... END; blocks -- each string literal is one assembly instruction. Emit the strings verbatim into the .s output at the current code position. Handle variable references in asm strings (simple substitution of stack offset expressions). Parse PROC OPTIONS(NAKED) -- skip prologue/epilogue generation. Test: ASM block that writes to LED MMIO address, naked interrupt handler stub.