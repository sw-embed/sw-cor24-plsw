Implemented %DEFINE, %IF, %ELSE, %ENDIF conditional compilation directives. Define table (32 entries) stores name/value pairs. Condition evaluator supports DEFINED(NAME), NAME = VALUE equality, and bare name shorthand. Conditional stack (8 levels) tracks active/skipped state for nested blocks. Refactored lex_try_include into general lex_try_directive handler. False branches skip tokens intelligently (strings, identifiers, numbers). 9 tests in suite #30 all pass: true/false branches, %ELSE both directions, value equality, nested %IF, false outer suppression, bare names, %IF+%INCLUDE interaction.