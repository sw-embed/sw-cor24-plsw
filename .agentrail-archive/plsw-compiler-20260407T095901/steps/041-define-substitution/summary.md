Implemented %DEFINE value substitution in expressions (GitHub issue #1). Added lexer-level substitution in lex_scan: when an identifier matches a define with a non-empty value, numeric values produce TOK_NUM tokens and text values are re-looked up as keywords/identifiers. Flag-only defines unchanged. Added 4 tests (numeric, keyword, large number, flag-only), define.plsw example, and usage docs.