Implemented scoped symbol table (symtab.h) with parallel arrays, arena allocation, global+per-procedure scopes, case-insensitive lookup, shadowing, duplicate detection, and symbol flags. 15 test cases all pass on COR24: global insert/lookup, cross-scope visibility, shadowing with correct inner-first resolution, local-only lookup, nested 2-level scopes, duplicate error, proc/array/record flags, and stack offsets.