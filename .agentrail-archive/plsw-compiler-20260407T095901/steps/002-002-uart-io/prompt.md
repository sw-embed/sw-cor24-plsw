Write UART I/O functions in C for the compiler: uart_putchar, uart_getchar, uart_puts, uart_getline (with backspace support). Write print_int for decimal integer output, parse_int for decimal string to integer. These are the compiler's own I/O routines (it runs on COR24). Test: print a startup banner and echo typed input. Reference sw-cor24-apl src/ for working UART code patterns.