Defined 50 token type codes and 27-entry keyword table for PL/SW lexer in src/token.h. Token types cover keywords, type names, storage classes, literals, operators, and punctuation. Keyword lookup via case-insensitive linear search using str_eq_nocase(). Includes tok_name() for debug output. Test suite in main.c validates all keyword matches, case-insensitive lookup, identifier classification, and tok_name formatting. All tests pass on COR24 emulator (23672 bytes assembled).