Fix bitwise AND (&) causing 'undefined variable' in codegen (GitHub issue #14). The issue says & causes 'undefined variable' which is odd since cg_binop already has TOK_AMP handling (we added it alongside TOK_AND in step 045). The error 'undefined variable' suggests the problem is elsewhere -- perhaps the expression parser is not creating a BINOP node for & but instead treating it as something else, or cg_is_simple is misidentifying the operand. Investigate by reproducing the error with a simple test: 'DCL X INT; DCL Y INT; X = Y & 3;' compiled via compile_program. Check what AST node the parser produces for & expressions, and trace through cg_expr to find where 'undefined variable' comes from.