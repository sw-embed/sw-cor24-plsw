Implemented pointer and address code generation: ADDR(var) computes address-of for stack/static/field/array targets, ptr->field dereference for load/store through pointers with field offset lookup via type descriptors. Fixed layout_last_tdesc reset bug that prevented PTR-to-record association. Added 4 tests covering ADDR of locals, ADDR of records with ptr->field store, ptr->field reads in expressions, and pointer arithmetic.