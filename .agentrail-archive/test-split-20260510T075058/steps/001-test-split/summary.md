Phase 1 of shrink-lgo: extracted 46 test_* functions and run_suite() from src/main.c (which is what tc24r compiles into the shipped plsw.lgo) into a new src/test_main.c (which builds a separate plsw_test.lgo). Moved shared compile_program() and emit_runtime_* into src/compile.h so both binaries reuse them. Added just build-test-lgo target. Production plsw.lgo shrank 1,738,122 -> 1,511,582 bytes (-226 KB / 13%). Smaller than the brief's ~900 KB speculation because the 6,800 test lines compiled to only ~226 KB of .text (mostly uart_puts calls and fixture strings, not dense code). reg-rs 15/15 green. Phase 2 (chunk allocator) is the next saga.